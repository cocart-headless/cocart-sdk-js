# Increase warning level to only show critical warnings
loglevel=error

# Use legacy peer dependencies mode (reduces some warnings)
legacy-peer-deps=true

# Force resolution of some dependencies to newer versions
resolution[glob]=^9.0.0
resolution[rimraf]=^5.0.0 